const db = require('../model/db');
const ACTIONS_ID = {

}

const create = function(bookId, userId, actionId) {
}

const delete = function(bookId, userId, actionId) {

}

module.exports = {
	ACTIONS_ID,
	create
}